package services

func Convert(s string, numRows int) string {
	hashMap := make(map[int]rune)
	for index, c := range s {
		hashMap[index] = c
	}
	var ss string
	for _, i := range hashMap {

	}
	return ""
}
